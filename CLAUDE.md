# Stock AnalyticsV2

Cloudflare-native personal Taiwan stock **event radar** — research discovery ("why did this stock surface"), NOT trading advice. Worker (D1) API + React/Vite dashboard + shared parsers/scoring. See `README.md` for routes, sources, and deployment.

## Agent skills

### Issue tracker

Issues and PRDs live as local markdown under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles; for local markdown the label is a `Status:` line in the issue file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at repo root (created lazily). See `docs/agents/domain.md`.
